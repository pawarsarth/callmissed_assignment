# Get this from app.callmissed.com -> Profile -> API Keys
# Give the key "llm" and "image" permissions.
# NEVER commit your real key. This file only documents the variable name.
CALLMISSED_API_KEY=cm_your_key_here
